name: Promote and Deploy

# Spec: work/items/task.0376 (per-node lane isolation, preview + production matrix).
# Companion to candidate-flight.yml (task.0372). Same primitive: matrix-fanned
# per-node cells push to deploy/<env>-<node>; aggregator computes current-sha
# from merge-base of per-node tips and merges source-sha-by-app.json rollup.

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Target environment"
        required: true
        default: preview
        type: choice
        options: [preview, production]
      source_sha:
        description: "SHA of source images. Empty for production = preview-forward mode (per-node, reads deploy/preview-<node>)."
        required: false
      build_sha:
        description: "SHA baked into images at build time (PR branch head). Defaults to source_sha. Ignored in preview-forward mode."
        required: false
      node_source_sha:
        description: "Remote-source (fork) node image sha — source-addressed like candidate-flight. REQUIRED to promote a remote-source node (preview AND production); the resolver pins this sha. The catalog source_sha is birth-only metadata, never a deploy authority — a remote-source promote that omits this hard-fails (bug.5043). Not used by the preview-forward path (production w/ no shas reads deploy/preview-<node>)."
        required: false
      nodes:
        description: "CSV of nodes to promote (default: all NODE_TARGETS + scheduler-worker). Set by flight-preview.yml from RESOLVED_TARGETS."
        required: false
        default: ""
      skip_infra:
        description: "Skip deploy-infra (Compose) step. Default true: app-digest promotion is orthogonal to substrate (mirrors candidate-flight, which has no deploy-infra job). Set false ONLY when the promoted diff touches infra/compose/**, an ESO/OpenBao secret the VM must materialize, or edge/runtime topology."
        required: false
        default: "true"
        type: choice
        options: ["true", "false"]
      deploy_infra_mode:
        description: "deploy-infra mode. full reconciles Compose + k8s bridge secrets; k8s-secrets-only syncs pod secrets without Compose."
        required: false
        default: full
        type: choice
        options: [full, k8s-secrets-only]

permissions:
  contents: write
  packages: read
  actions: read

# Per-(env, nodes) lane (bug.5022). The operator-API prod-promote path dispatches
# one node per run (github-repo-write.ts: `nodes: input.slug`), so an env-only group
# let N near-simultaneous node promotes collide: with cancel-in-progress:false GitHub
# keeps only ONE run pending per group, so the 3rd dispatch cancels the 2nd while it
# queues. Including `inputs.nodes` gives each node its own lane. Same-node races stay
# serialized by the per-(env,node) job groups (flight-/reconcile-appset-/node-substrate-).
concurrency:
  group: promote-deploy-${{ inputs.environment || github.ref_name }}-${{ inputs.nodes }}
  cancel-in-progress: false

jobs:
  decide:
    runs-on: ubuntu-latest
    outputs:
      environment: ${{ steps.env.outputs.environment }}
      head_sha: ${{ steps.env.outputs.head_sha }}
      build_sha: ${{ steps.env.outputs.build_sha }}
      deploy_branch: ${{ steps.env.outputs.deploy_branch }}
      preview_forward: ${{ steps.env.outputs.preview_forward }}
      targets_json: ${{ steps.targets.outputs.targets_json }}
      has_targets: ${{ steps.targets.outputs.has_targets }}
      node_targets_json: ${{ steps.targets.outputs.node_targets_json }}
      has_node_targets: ${{ steps.targets.outputs.has_node_targets }}
    steps:
      - name: Checkout workflow source
        uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955 # v4
        with:
          fetch-depth: 1

      - name: Verify deployment parent
        run: bash scripts/ci/assert-deployment-parent.sh "${{ inputs.environment }}"

      - name: Determine environment
        id: env
        run: |
          set -euo pipefail
          ENV="${{ inputs.environment }}"
          RAW_SOURCE_SHA="${{ inputs.source_sha }}"
          PREVIEW_FORWARD=false
          # Preview-forward only when NEITHER a source_sha NOR a node_source_sha is given.
          # A fork-node production promote is SOURCE-ADDRESSED via node_source_sha (no
          # source_sha); it must NOT be hijacked into preview-forward, or the digest job
          # deploys the stale deploy/preview-<node> pin while node-substrate materializes
          # the requested sha — a silent split-brain verify-buildsha can't catch because it
          # self-confirms the preview pin it wrote (bug.5043). node_source_sha present ⇒
          # preview_forward=false ⇒ digest reaches the remote-source branch that honors it.
          if [ "$ENV" = "production" ] && [ -z "$RAW_SOURCE_SHA" ] && [ -z "${{ inputs.node_source_sha }}" ]; then
            PREVIEW_FORWARD=true
            RAW_SOURCE_SHA="${{ github.sha }}"
          fi
          SHA="${RAW_SOURCE_SHA:-${{ github.sha }}}"
          BUILD_SHA="${{ inputs.build_sha }}"
          BUILD_SHA="${BUILD_SHA:-$SHA}"
          case "$ENV" in
            preview)    DEPLOY_BRANCH=deploy/preview ;;
            production) DEPLOY_BRANCH=deploy/production ;;
            *)          echo "❌ Unknown environment: $ENV"; exit 1 ;;
          esac
          {
            echo "environment=$ENV"
            echo "head_sha=$SHA"
            echo "build_sha=$BUILD_SHA"
            echo "deploy_branch=$DEPLOY_BRANCH"
            echo "preview_forward=$PREVIEW_FORWARD"
          } >> "$GITHUB_OUTPUT"

      - name: Checkout (for image-tags lib)
        uses: actions/checkout@v4
        with:
          ref: ${{ steps.env.outputs.head_sha }}
          fetch-depth: 1

      - name: Resolve target node list
        id: targets
        env:
          NODES_INPUT: ${{ inputs.nodes }}
          OVERLAY_ENV: ${{ steps.env.outputs.environment }}
        run: bash scripts/ci/resolve-promote-targets.sh

  reconcile-appset:
    needs: decide
    if: needs.decide.outputs.has_targets == 'true'
    # Per-node lane (axiom 18). Apply ONLY the promoted node's ApplicationSet
    # object so a concurrent promotion of a different node cannot prune this lane.
    # Structural fix for the shared-appset clobber — mirrors candidate-flight.yml.
    strategy:
      fail-fast: false
      # Every matrix leg SSHes the SAME env VM. Un-capped, a full-fleet promote
      # fires ~11 parallel SSH sessions at one sshd in the same second and trips
      # MaxStartups (default 10:30:100) → the overflow legs get `kex_exchange_
      # identification: Connection reset by peer` → exit 255 (NOT auth/VM-down;
      # 8/11 succeed, ~3 random victims). Cap concurrency well under MaxStartups
      # so the herd never forms. (Retry below covers any residual transient.)
      max-parallel: 3
      matrix:
        node: ${{ fromJson(needs.decide.outputs.targets_json) }}
    concurrency:
      group: reconcile-appset-${{ needs.decide.outputs.environment }}-${{ matrix.node }}
      cancel-in-progress: false
    runs-on: ubuntu-latest
    environment: ${{ needs.decide.outputs.environment }}
    steps:
      # Checkout the head ref — the per-node AppSet YAML is catalog-derived CI
      # control plane (CATALOG_IS_SSOT), and a node ADDED in this promote carries
      # its own AppSet file at head.
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.decide.outputs.head_sha }}

      - name: Setup SSH
        id: ssh-setup
        env:
          VM_HOST: ${{ secrets.VM_HOST }}
          SSH_DEPLOY_KEY: ${{ secrets.SSH_DEPLOY_KEY }}
        run: |
          if [ -z "$VM_HOST" ]; then
            echo "⚠️ No VM_HOST — skipping AppSet reconcile"
            echo "has_vm=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          echo "has_vm=true" >> "$GITHUB_OUTPUT"
          mkdir -p ~/.ssh && chmod 700 ~/.ssh
          echo "$SSH_DEPLOY_KEY" | tr -d '\r' > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -T 10 -H "$VM_HOST" >> ~/.ssh/known_hosts 2>/dev/null || true

      - name: Apply ${{ needs.decide.outputs.environment }}-${{ matrix.node }}-applicationset.yaml
        if: steps.ssh-setup.outputs.has_vm == 'true'
        env:
          VM_HOST: ${{ secrets.VM_HOST }}
          SSH_OPTS: "-i /home/runner/.ssh/deploy_key -o StrictHostKeyChecking=accept-new -o ConnectTimeout=30 -o ServerAliveInterval=10 -o ServerAliveCountMax=6"
          DEPLOY_ENVIRONMENT: ${{ needs.decide.outputs.environment }}
          NODE: ${{ matrix.node }}
        run: |
          set -euo pipefail
          APPSET_FILE="infra/k8s/argocd/appsets/${DEPLOY_ENVIRONMENT}/${DEPLOY_ENVIRONMENT}-${NODE}-applicationset.yaml"
          if [ ! -f "$APPSET_FILE" ]; then
            echo "::error::$APPSET_FILE missing at head — run 'pnpm gen:node-appset' (CATALOG_IS_SSOT)"
            exit 1
          fi
          # Retry transient SSH failures (e.g. a MaxStartups "Connection reset"
          # that slips past the max-parallel cap) with backoff. Idempotent target,
          # so a retried apply is safe.
          retry() {
            local n=0 max=5 delay=5
            until "$@"; do
              n=$((n + 1))
              if [ "$n" -ge "$max" ]; then
                echo "::error::SSH step failed after ${max} attempts"
                return 1
              fi
              echo "::warning::attempt ${n} failed (transient SSH?) — retrying in ${delay}s"
              sleep "$delay"
              delay=$((delay * 2))
            done
          }
          # shellcheck disable=SC2086
          retry scp $SSH_OPTS "$APPSET_FILE" root@"$VM_HOST":/tmp/appset-${DEPLOY_ENVIRONMENT}-${NODE}.yaml
          # Anti-resurrection: orphan-delete the retired shared `cogni-<env>` AppSet
          # (keeps its live Applications), then apply the per-node object.
          # shellcheck disable=SC2086
          retry ssh $SSH_OPTS root@"$VM_HOST" "kubectl delete applicationset cogni-${DEPLOY_ENVIRONMENT} -n argocd --cascade=orphan --ignore-not-found && kubectl apply -f /tmp/appset-${DEPLOY_ENVIRONMENT}-${NODE}.yaml -n argocd && rm -f /tmp/appset-${DEPLOY_ENVIRONMENT}-${NODE}.yaml"

  # Per-node public DNS for preview + production — mirrors candidate-flight.yml's
  # reconcile-dns. DNS is env-level + idempotent (no-op for existing nodes); a node
  # newly added to the catalog gets its `<node>-<env host>` A record on its FIRST
  # promote, so verify-deploy can probe its public /version. Without this, per-node
  # DNS only existed on candidate-a and node formation dead-ended one env up.
  # Provision happens inside the promote lane (idempotent), not a full deploy-infra
  # env reprovision. Serialize Cloudflare writes per env (mirrors reconcile-appset).
  reconcile-dns:
    needs: decide
    if: needs.decide.outputs.has_targets == 'true'
    concurrency:
      group: reconcile-dns-${{ needs.decide.outputs.environment }}
      cancel-in-progress: false
    runs-on: ubuntu-latest
    environment: ${{ needs.decide.outputs.environment }}
    steps:
      # Promote head, so a node ADDED in this promote carries its catalog entry +
      # the reconcile script (CATALOG_IS_SSOT).
      - name: Checkout app source (catalog + scripts)
        uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955 # v4
        with:
          ref: ${{ needs.decide.outputs.head_sha }}

      - name: Reconcile node DNS (per-node A records → VM IP)
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ZONE_ID: ${{ secrets.CLOUDFLARE_ZONE_ID }}
          FORK_DOMAIN_ROOT: ${{ vars.FORK_DOMAIN_ROOT || 'cognidao.org' }}
          DOMAIN: ${{ vars.DOMAIN }}
          DEPLOY_ENVIRONMENT: ${{ needs.decide.outputs.environment }}
        run: |
          set -euo pipefail
          # A pre-#1445 promote head lacks this script; skip rather than 127.
          if [ ! -f scripts/ci/reconcile-node-dns.sh ]; then
            echo "::warning::scripts/ci/reconcile-node-dns.sh absent at this ref — skipping node DNS reconcile"
            exit 0
          fi
          if [ -z "${CLOUDFLARE_API_TOKEN:-}" ] || [ -z "${CLOUDFLARE_ZONE_ID:-}" ]; then
            echo "::warning::Cloudflare secrets unset on ${DEPLOY_ENVIRONMENT} — skipping node DNS reconcile (newly-created nodes won't resolve)"
            exit 0
          fi
          bash scripts/ci/reconcile-node-dns.sh "${DEPLOY_ENVIRONMENT}"

  promote-k8s:
    needs: [decide, reconcile-appset, node-substrate]
    if: |
      always() &&
      needs.decide.outputs.has_targets == 'true' &&
      needs.reconcile-appset.result == 'success' &&
      (
        (needs.decide.outputs.has_node_targets == 'true' && needs.node-substrate.result == 'success') ||
        (needs.decide.outputs.has_node_targets != 'true' && needs.node-substrate.result == 'skipped')
      )
    strategy:
      fail-fast: false
      matrix:
        node: ${{ fromJson(needs.decide.outputs.targets_json) }}
    concurrency:
      group: flight-${{ needs.decide.outputs.environment }}-${{ matrix.node }}
      cancel-in-progress: false
    runs-on: ubuntu-latest
    environment: ${{ needs.decide.outputs.environment }}
    steps:
      - name: Checkout app source
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.decide.outputs.head_sha }}
          fetch-depth: 1
          token: ${{ secrets.ACTIONS_AUTOMATION_BOT_PAT }}
          path: app-src

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Checkout per-node preview branch (preview-forward mode)
        if: needs.decide.outputs.preview_forward == 'true'
        uses: actions/checkout@v4
        with:
          ref: deploy/preview-${{ matrix.node }}
          fetch-depth: 1
          token: ${{ secrets.ACTIONS_AUTOMATION_BOT_PAT }}
          path: preview-src

      - name: Resolve digest for this node
        id: digest
        env:
          NODE: ${{ matrix.node }}
          HEAD_SHA: ${{ needs.decide.outputs.head_sha }}
          BUILD_SHA: ${{ needs.decide.outputs.build_sha }}
          PREVIEW_FORWARD: ${{ needs.decide.outputs.preview_forward }}
          NODE_SOURCE_SHA: ${{ inputs.node_source_sha }}
        run: |
          set -euo pipefail
          # shellcheck source=scripts/ci/lib/image-tags.sh
          . app-src/scripts/ci/lib/image-tags.sh
          if [ "$PREVIEW_FORWARD" = "true" ]; then
            # Reuse the existing per-overlay digest resolver (battle-tested);
            # it emits digests_json={...} for ALL_TARGETS, then we index by NODE.
            PREVIEW_OVERLAY_ROOT="${{ github.workspace }}/preview-src/infra/k8s/overlays/preview"
            export PREVIEW_OVERLAY_ROOT
            DIGESTS_JSON=$(bash app-src/scripts/ci/resolve-digests-from-preview.sh \
              | grep '^digests_json=' | head -1 | sed 's/^digests_json=//')
            if [ -z "$DIGESTS_JSON" ]; then
              echo "::error::preview-forward: resolve-digests-from-preview emitted no digests_json"
              exit 1
            fi
            DIGEST=$(printf '%s' "$DIGESTS_JSON" | jq -r --arg k "$NODE" '.[$k] // ""')
            if [ -z "$DIGEST" ]; then
              echo "::error::preview-forward: no digest for $NODE in deploy/preview-$NODE"
              exit 1
            fi
            APP_SOURCE_SHA=$(jq -r --arg k "$NODE" '.[$k] // ""' "${{ github.workspace }}/preview-src/.promote-state/source-sha-by-app.json")
            if [ -z "$APP_SOURCE_SHA" ]; then
              echo "::error::preview-forward: no source_sha for $NODE in preview map"
              exit 1
            fi
          elif is_remote_source_artifact_target "$NODE"; then
            catalog="app-src/infra/catalog/${NODE}.yaml"
            image_repository="$(yq -N '.image_repository // ""' "$catalog")"
            if [ -z "$image_repository" ]; then
              echo "::error::remote-source artifact ${NODE} missing image_repository"
              exit 1
            fi
            # Source-addressed ONLY (ONE_PROMOTION_PRIMITIVE, like candidate-flight): a
            # remote-source node MUST carry node_source_sha on the dispatch — every rung
            # (candidate/preview/production) source-addresses via the operator (`promoteNode`).
            # The catalog source_sha is birth-only metadata, NEVER a deploy authority (bug.5043);
            # a dispatch that omits the input hard-fails LOUDLY rather than silently deploying the
            # stale pin. This APP_SOURCE_SHA is both the image tag below AND the value recorded into
            # .promote-state/source-sha-by-app.json (update-source-sha-map.sh), so the deploy/<env>
            # pin always matches what was deployed.
            if [ -z "${NODE_SOURCE_SHA:-}" ]; then
              echo "::error::remote-source artifact ${NODE} requires node_source_sha (source-addressed promote); catalog source_sha is birth-only, not a deploy pin (bug.5043)"
              exit 1
            fi
            APP_SOURCE_SHA="$NODE_SOURCE_SHA"
            full_tag="${image_repository}:sha-${APP_SOURCE_SHA}"
            digest_only=$(docker buildx imagetools inspect "$full_tag" --raw 2>/dev/null \
              | python3 -c "import json,sys; print(json.load(sys.stdin).get('digest',''))" 2>/dev/null || true)
            if [ -z "$digest_only" ]; then
              digest_only=$(docker buildx imagetools inspect "$full_tag" 2>/dev/null \
                | grep -oP 'Digest:\s+\Ksha256:[a-f0-9]+' | head -1 || true)
            fi
            if [ -z "$digest_only" ]; then
              echo "::error::remote-source artifact image not found: $full_tag"
              exit 1
            fi
            DIGEST="${full_tag%%:*}@${digest_only}"
          else
            # In-repo artifact (operator, scheduler-worker): ONE identity —
            # `<image>:sha-<sourceSha>{suffix}` (SOURCE_SHA_IS_DEPLOY_IDENTITY).
            # The sha IS the BUILD_SHA baked into the image, which verify-buildsha
            # asserts — so the tag-sha, the recorded source_sha, and /version.buildSha
            # are the same value. (Was the legacy preview-{sha} re-tag, now purged.)
            full_tag=$(image_tag_for_target "$(image_name_for_target "$NODE")" "sha-${BUILD_SHA}" "$NODE")
            digest_only=$(docker buildx imagetools inspect "$full_tag" --raw 2>/dev/null \
              | python3 -c "import json,sys; print(json.load(sys.stdin).get('digest',''))" 2>/dev/null || true)
            if [ -z "$digest_only" ]; then
              digest_only=$(docker buildx imagetools inspect "$full_tag" 2>/dev/null \
                | grep -oP 'Digest:\s+\Ksha256:[a-f0-9]+' | head -1 || true)
            fi
            if [ -z "$digest_only" ]; then
              echo "::warning::Image not found: $full_tag (not built — skipping $NODE)"
              echo "digest=" >> "$GITHUB_OUTPUT"
              echo "promoted=false" >> "$GITHUB_OUTPUT"
              exit 0
            fi
            DIGEST="${full_tag%%:*}@${digest_only}"
            APP_SOURCE_SHA="$BUILD_SHA"
          fi
          echo "digest=$DIGEST" >> "$GITHUB_OUTPUT"
          echo "source_sha=$APP_SOURCE_SHA" >> "$GITHUB_OUTPUT"
          echo "promoted=true" >> "$GITHUB_OUTPUT"

      - name: Prepare per-node deploy branch (bootstrap from umbrella if missing)
        if: steps.digest.outputs.promoted == 'true'
        env:
          GH_TOKEN: ${{ secrets.ACTIONS_AUTOMATION_BOT_PAT }}
          PER_NODE_BRANCH: deploy/${{ needs.decide.outputs.environment }}-${{ matrix.node }}
          UMBRELLA_BRANCH: deploy/${{ needs.decide.outputs.environment }}
        run: |
          set -euo pipefail
          git clone "https://x-access-token:${GH_TOKEN}@github.com/${{ github.repository }}.git" deploy-branch
          cd deploy-branch
          git fetch origin "+refs/heads/deploy/*:refs/remotes/origin/deploy/*"
          if git rev-parse --verify --quiet "refs/remotes/origin/${PER_NODE_BRANCH}" >/dev/null; then
            git checkout -B "${PER_NODE_BRANCH}" "origin/${PER_NODE_BRANCH}"
          else
            echo "::warning::Per-node branch ${PER_NODE_BRANCH} missing — bootstrapping from ${UMBRELLA_BRANCH}"
            git checkout -B "${PER_NODE_BRANCH}" "origin/${UMBRELLA_BRANCH}"
          fi
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Sync infra/k8s + per-node catalog from app-src
        if: steps.digest.outputs.promoted == 'true'
        env:
          NODE: ${{ matrix.node }}
          OVERLAY_ENV: ${{ needs.decide.outputs.environment }}
        run: |
          set -euo pipefail
          # env-state.yaml protection (bug.0334) — preserve VM-truth files;
          # rewrite everything else under infra/k8s/ from app-src.
          rsync -a --ignore-existing \
            --include='*/' --include='env-state.yaml' --exclude='*' \
            app-src/infra/k8s/ deploy-branch/infra/k8s/
          rsync -a --delete --exclude='env-state.yaml' \
            app-src/infra/k8s/base/ deploy-branch/infra/k8s/base/
          mkdir -p "deploy-branch/infra/k8s/overlays/${OVERLAY_ENV}/${NODE}"
          rsync -a --delete --exclude='env-state.yaml' \
            "app-src/infra/k8s/overlays/${OVERLAY_ENV}/${NODE}/" \
            "deploy-branch/infra/k8s/overlays/${OVERLAY_ENV}/${NODE}/"
          mkdir -p deploy-branch/infra/catalog
          cp "app-src/infra/catalog/${NODE}.yaml" "deploy-branch/infra/catalog/${NODE}.yaml"
          if [ -d "app-src/nodes/${NODE}/k8s/external-secrets/${OVERLAY_ENV}" ]; then
            mkdir -p "deploy-branch/nodes/${NODE}/k8s/external-secrets/${OVERLAY_ENV}"
            rsync -a --delete \
              "app-src/nodes/${NODE}/k8s/external-secrets/${OVERLAY_ENV}/" \
              "deploy-branch/nodes/${NODE}/k8s/external-secrets/${OVERLAY_ENV}/"
          fi

      - name: Promote digest + record source-sha
        if: steps.digest.outputs.promoted == 'true'
        working-directory: deploy-branch
        env:
          OVERLAY_ENV: ${{ needs.decide.outputs.environment }}
          NODE: ${{ matrix.node }}
          DIGEST: ${{ steps.digest.outputs.digest }}
          SOURCE_SHA: ${{ steps.digest.outputs.source_sha }}
        run: |
          set -euo pipefail
          ../app-src/scripts/ci/promote-k8s-image.sh --no-commit \
            --env "$OVERLAY_ENV" --app "$NODE" --digest "$DIGEST"
          APP="$NODE" SOURCE_SHA="$SOURCE_SHA" \
            bash ../app-src/scripts/ci/update-source-sha-map.sh

      - name: Commit and push
        id: push
        if: steps.digest.outputs.promoted == 'true'
        working-directory: deploy-branch
        env:
          PER_NODE_BRANCH: deploy/${{ needs.decide.outputs.environment }}-${{ matrix.node }}
          NODE: ${{ matrix.node }}
          HEAD_SHA: ${{ needs.decide.outputs.head_sha }}
          OVERLAY_ENV: ${{ needs.decide.outputs.environment }}
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            DEPLOY_SHA=$(git rev-parse HEAD)
            echo "deploy_branch_sha=$DEPLOY_SHA" >> "$GITHUB_OUTPUT"
            echo "ℹ️  No diff for ${NODE} — deploy_branch_sha=${DEPLOY_SHA}"
            exit 0
          fi
          SHORT=$(echo "$HEAD_SHA" | cut -c1-8)
          git commit -m "promote ${OVERLAY_ENV} ${NODE}: ${SHORT}"
          git push origin "HEAD:${PER_NODE_BRANCH}"
          DEPLOY_SHA=$(git rev-parse HEAD)
          echo "deploy_branch_sha=$DEPLOY_SHA" >> "$GITHUB_OUTPUT"

      - name: Upload per-cell artifact
        if: always()
        env:
          NODE: ${{ matrix.node }}
          DEPLOY_SHA: ${{ steps.push.outputs.deploy_branch_sha }}
          PROMOTED: ${{ steps.digest.outputs.promoted }}
        run: |
          mkdir -p out
          printf '%s' "${DEPLOY_SHA:-}" > "out/deploy-sha-${NODE}.txt"
          printf '%s' "${PROMOTED:-false}" > "out/promoted-${NODE}.txt"
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: always()
        with:
          name: cell-${{ matrix.node }}
          path: out/
          retention-days: 1

  # Per-node substrate (materialize -> reconcile) per-env BEFORE pods, on EVERY env
  # (Invariant 16). One job calls run-node-substrate.sh for each type:node target,
  # uniform across candidate-a/preview/production with no special-case.
  # Orchestration only; logic lives in run-node-substrate.sh.
  node-substrate:
    needs: [decide, reconcile-appset, reconcile-dns]
    if: |
      always() &&
      needs.decide.outputs.has_node_targets == 'true' &&
      needs.reconcile-appset.result == 'success' &&
      needs.reconcile-dns.result == 'success'
    strategy:
      fail-fast: false
      # Same shared-VM SSH fan-out as reconcile-appset (run-node-substrate.sh
      # SSHes the one env VM). Cap concurrency so a full-fleet promote can't
      # trip sshd MaxStartups (see reconcile-appset above).
      max-parallel: 3
      matrix:
        node: ${{ fromJson(needs.decide.outputs.node_targets_json) }}
    concurrency:
      group: node-substrate-${{ needs.decide.outputs.environment }}-${{ matrix.node }}
      cancel-in-progress: false
    runs-on: ubuntu-latest
    environment: ${{ needs.decide.outputs.environment }}
    steps:
      - name: Checkout app source
        uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955 # v4
        with:
          ref: ${{ needs.decide.outputs.head_sha }}
          fetch-depth: 0
          token: ${{ secrets.ACTIONS_AUTOMATION_BOT_PAT || github.token }}
          path: app-src

      - name: Materialize node source at the dispatched node_source_sha
        env:
          GH_TOKEN: ${{ secrets.GIT_READ_TOKEN || github.token }}
          NODE: ${{ matrix.node }}
          NODE_SOURCE_SHA: ${{ inputs.node_source_sha }}
        run: |
          set -euo pipefail
          # Remote-source node: fetch its tree into nodes/<NODE> — no submodule,
          # no gitlink, private-safe (GIT_READ_TOKEN). In-repo nodes carry no
          # source_repo and are already in app-src. spec.node-submodule-retirement.
          catalog="app-src/infra/catalog/${NODE}.yaml"
          [ -f "$catalog" ] || exit 0
          src_repo="$(yq -N '.source_repo // ""' "$catalog")"
          [ -n "$src_repo" ] || exit 0
          # Source-addressed ONLY (ONE_PROMOTION_PRIMITIVE) — the SAME contract as the digest job:
          # a remote-source node MUST carry node_source_sha. The catalog source_sha is birth-only
          # metadata, NEVER a deploy authority (bug.5043). Hard-fail a dispatch that omits it rather
          # than fetching the tree at the stale pin (which would split-brain vs the digest job).
          src_sha="${NODE_SOURCE_SHA:-}"
          [ -n "$src_sha" ] || { echo "::error::remote-source node ${NODE} requires node_source_sha (source-addressed promote); catalog source_sha is birth-only, not a deploy pin (bug.5043)"; exit 1; }
          repo_path="${src_repo#https://github.com/}"; repo_path="${repo_path%.git}"
          rm -rf "app-src/nodes/${NODE}"
          git init -q "app-src/nodes/${NODE}"
          git -C "app-src/nodes/${NODE}" remote add origin "https://x-access-token:${GH_TOKEN}@github.com/${repo_path}.git"
          git -C "app-src/nodes/${NODE}" fetch -q --depth 1 origin "$src_sha"
          git -C "app-src/nodes/${NODE}" checkout -q --detach FETCH_HEAD

      - name: Setup SSH
        env:
          VM_HOST: ${{ secrets.VM_HOST }}
          SSH_DEPLOY_KEY: ${{ secrets.SSH_DEPLOY_KEY }}
        run: |
          set -euo pipefail
          if [ -z "$VM_HOST" ]; then
            echo "::error::VM_HOST is required for node substrate"
            exit 1
          fi
          mkdir -p ~/.ssh && chmod 700 ~/.ssh
          echo "$SSH_DEPLOY_KEY" | tr -d '\r' > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -T 10 -H "$VM_HOST" >> ~/.ssh/known_hosts 2>/dev/null || true

      - name: Run node substrate (materialize -> reconcile)
        env:
          VM_HOST: ${{ secrets.VM_HOST }}
          SSH_OPTS: "-i /home/runner/.ssh/deploy_key -o StrictHostKeyChecking=accept-new -o ConnectTimeout=30 -o ServerAliveInterval=10 -o ServerAliveCountMax=6"
          DEPLOY_ENVIRONMENT: ${{ needs.decide.outputs.environment }}
          DOMAIN: ${{ vars.DOMAIN }}
          APP_SOURCE_DIR: app-src
          COGNI_CATALOG_ROOT: app-src/infra/catalog
        run: bash app-src/scripts/ci/run-node-substrate.sh "${{ needs.decide.outputs.environment }}" "${{ matrix.node }}"

  deploy-infra:
    if: |
      always() &&
      inputs.skip_infra != 'true' &&
      needs.decide.outputs.has_targets == 'true' &&
      needs.promote-k8s.result == 'success' &&
      (
        (needs.decide.outputs.has_node_targets == 'true' && needs.node-substrate.result == 'success') ||
        (needs.decide.outputs.has_node_targets != 'true' && needs.node-substrate.result == 'skipped')
      )
    runs-on: ubuntu-latest
    needs: [decide, promote-k8s, node-substrate]
    environment: ${{ needs.decide.outputs.environment }}
    # 45m (was 15m, #785-era default): a cold-cache VM provisioning a NEW node
    # pulls the sandbox + LiteLLM images (~13m) BEFORE db-provision runs, so 15m
    # times out before `cogni_node_template` is created — node-template-preview
    # 502'd indefinitely. candidate-flight-infra.yml has no cap (360m default),
    # which is why candidate-a never hit this. Bounded bump, not unbounded.
    timeout-minutes: 45
    concurrency:
      group: deploy-infra-${{ needs.decide.outputs.environment }}
      cancel-in-progress: false
    env:
      VM_HOST: ${{ secrets.VM_HOST }}
      DOMAIN: ${{ vars.DOMAIN }}
      DEPLOY_ENVIRONMENT: ${{ needs.decide.outputs.environment }}
      APP_ENV: production
      COGNI_REPO_URL: https://github.com/${{ github.repository }}.git
      COGNI_REPO_REF: ${{ needs.decide.outputs.head_sha }}
      GIT_READ_USERNAME: Cogni-1729
      GIT_READ_TOKEN: ${{ secrets.GIT_READ_TOKEN }}
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      DATABASE_SERVICE_URL: ${{ secrets.DATABASE_SERVICE_URL }}
      POSTGRES_ROOT_USER: ${{ secrets.POSTGRES_ROOT_USER }}
      POSTGRES_ROOT_PASSWORD: ${{ secrets.POSTGRES_ROOT_PASSWORD }}
      APP_DB_USER: ${{ secrets.APP_DB_USER }}
      APP_DB_PASSWORD: ${{ secrets.APP_DB_PASSWORD }}
      APP_DB_SERVICE_USER: ${{ secrets.APP_DB_SERVICE_USER }}
      APP_DB_SERVICE_PASSWORD: ${{ secrets.APP_DB_SERVICE_PASSWORD }}
      APP_DB_READONLY_USER: ${{ secrets.APP_DB_READONLY_USER }}
      APP_DB_READONLY_PASSWORD: ${{ secrets.APP_DB_READONLY_PASSWORD }}
      APP_DB_NAME: ${{ secrets.APP_DB_NAME }}
      LITELLM_MASTER_KEY: ${{ secrets.LITELLM_MASTER_KEY }}
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
      AUTH_SECRET: ${{ secrets.AUTH_SECRET }}
      EVM_RPC_URL: ${{ secrets.EVM_RPC_URL }}
      POLYGON_RPC_URL: ${{ secrets.POLYGON_RPC_URL }}
      TEMPORAL_DB_USER: ${{ secrets.TEMPORAL_DB_USER }}
      TEMPORAL_DB_PASSWORD: ${{ secrets.TEMPORAL_DB_PASSWORD }}
      GHCR_DEPLOY_TOKEN: ${{ secrets.GHCR_DEPLOY_TOKEN }}
      GHCR_USERNAME: Cogni-1729
      ACTIONS_AUTOMATION_BOT_PAT: ${{ secrets.ACTIONS_AUTOMATION_BOT_PAT }}
      GRAFANA_CLOUD_LOKI_URL: ${{ secrets.GRAFANA_CLOUD_LOKI_URL }}
      GRAFANA_CLOUD_LOKI_USER: ${{ secrets.GRAFANA_CLOUD_LOKI_USER }}
      GRAFANA_CLOUD_LOKI_API_KEY: ${{ secrets.GRAFANA_CLOUD_LOKI_API_KEY }}
      METRICS_TOKEN: ${{ secrets.METRICS_TOKEN }}
      PROMETHEUS_REMOTE_WRITE_URL: ${{ secrets.PROMETHEUS_REMOTE_WRITE_URL }}
      PROMETHEUS_USERNAME: ${{ secrets.PROMETHEUS_USERNAME }}
      PROMETHEUS_PASSWORD: ${{ secrets.PROMETHEUS_PASSWORD }}
      PROMETHEUS_QUERY_URL: ${{ secrets.PROMETHEUS_QUERY_URL }}
      PROMETHEUS_READ_USERNAME: ${{ secrets.PROMETHEUS_READ_USERNAME }}
      PROMETHEUS_READ_PASSWORD: ${{ secrets.PROMETHEUS_READ_PASSWORD }}
      LANGFUSE_PUBLIC_KEY: ${{ secrets.LANGFUSE_PUBLIC_KEY }}
      LANGFUSE_SECRET_KEY: ${{ secrets.LANGFUSE_SECRET_KEY }}
      LANGFUSE_BASE_URL: ${{ secrets.LANGFUSE_BASE_URL }}
      SCHEDULER_API_TOKEN: ${{ secrets.SCHEDULER_API_TOKEN }}
      BILLING_INGEST_TOKEN: ${{ secrets.BILLING_INGEST_TOKEN }}
      INTERNAL_OPS_TOKEN: ${{ secrets.INTERNAL_OPS_TOKEN }}
      DISCORD_BOT_TOKEN: ${{ secrets.DISCORD_BOT_TOKEN }}
      GH_OAUTH_CLIENT_ID: ${{ secrets.GH_OAUTH_CLIENT_ID }}
      GH_OAUTH_CLIENT_SECRET: ${{ secrets.GH_OAUTH_CLIENT_SECRET }}
      DISCORD_OAUTH_CLIENT_ID: ${{ secrets.DISCORD_OAUTH_CLIENT_ID }}
      DISCORD_OAUTH_CLIENT_SECRET: ${{ secrets.DISCORD_OAUTH_CLIENT_SECRET }}
      GOOGLE_OAUTH_CLIENT_ID: ${{ secrets.GOOGLE_OAUTH_CLIENT_ID }}
      GOOGLE_OAUTH_CLIENT_SECRET: ${{ secrets.GOOGLE_OAUTH_CLIENT_SECRET }}
      DOLTHUB_OWNER: ${{ vars.DOLTHUB_OWNER }}
      DOLT_CREDS_JWK: ${{ secrets.DOLT_CREDS_JWK }}
      DOLT_CREDS_KEYID: ${{ secrets.DOLT_CREDS_KEYID }}
      DOLTHUB_API_TOKEN: ${{ secrets.DOLTHUB_API_TOKEN }}
      DOLTHUB_OAUTH_CLIENT_ID: ${{ secrets.DOLTHUB_OAUTH_CLIENT_ID }}
      DOLTHUB_OAUTH_CLIENT_SECRET: ${{ secrets.DOLTHUB_OAUTH_CLIENT_SECRET }}
      GRAFANA_URL: ${{ secrets.GRAFANA_URL }}
      GRAFANA_SERVICE_ACCOUNT_TOKEN: ${{ secrets.GRAFANA_SERVICE_ACCOUNT_TOKEN }}
      GRAFANA_PDC_SIGNING_TOKEN: ${{ secrets.GRAFANA_PDC_SIGNING_TOKEN }}
      GRAFANA_PDC_HOSTED_GRAFANA_ID: ${{ secrets.GRAFANA_PDC_HOSTED_GRAFANA_ID }}
      GRAFANA_PDC_CLUSTER: ${{ secrets.GRAFANA_PDC_CLUSTER }}
      GRAFANA_PDC_NETWORK_ID: ${{ secrets.GRAFANA_PDC_NETWORK_ID }}
      GRAFANA_PDC_NETWORK_UUID: ${{ secrets.GRAFANA_PDC_NETWORK_UUID }}
      GH_REVIEW_APP_ID: ${{ secrets.GH_REVIEW_APP_ID }}
      GH_REVIEW_APP_PRIVATE_KEY_BASE64: ${{ secrets.GH_REVIEW_APP_PRIVATE_KEY_BASE64 }}
      NODE_MINT_OWNER: ${{ secrets.NODE_MINT_OWNER }}
      NODE_TEMPLATE_OWNER: ${{ secrets.NODE_TEMPLATE_OWNER }}
      GH_REPOS: ${{ vars.GH_REPOS }}
      GH_WEBHOOK_SECRET: ${{ secrets.GH_WEBHOOK_SECRET }}
      PRIVY_APP_ID: ${{ secrets.PRIVY_APP_ID }}
      PRIVY_APP_SECRET: ${{ secrets.PRIVY_APP_SECRET }}
      PRIVY_SIGNING_KEY: ${{ secrets.PRIVY_SIGNING_KEY }}
      PRIVY_USER_WALLETS_APP_ID: ${{ secrets.PRIVY_USER_WALLETS_APP_ID }}
      PRIVY_USER_WALLETS_APP_SECRET: ${{ secrets.PRIVY_USER_WALLETS_APP_SECRET }}
      PRIVY_USER_WALLETS_SIGNING_KEY: ${{ secrets.PRIVY_USER_WALLETS_SIGNING_KEY }}
      POLY_WALLET_AEAD_KEY_HEX: ${{ secrets.POLY_WALLET_AEAD_KEY_HEX }}
      POLY_WALLET_AEAD_KEY_ID: ${{ secrets.POLY_WALLET_AEAD_KEY_ID }}
      POLY_WALLET_ALLOW_STUB_CREDS: ${{ secrets.POLY_WALLET_ALLOW_STUB_CREDS }}
      POLY_CLOB_GEO_BLOCK_TOKEN: ${{ secrets.POLY_CLOB_GEO_BLOCK_TOKEN }}
      COPY_TRADE_TARGET_WALLETS: ${{ secrets.COPY_TRADE_TARGET_WALLETS }}
      CONNECTIONS_ENCRYPTION_KEY: ${{ secrets.CONNECTIONS_ENCRYPTION_KEY }}
      POSTHOG_API_KEY: ${{ secrets.POSTHOG_API_KEY }}
      POSTHOG_HOST: ${{ secrets.POSTHOG_HOST }}
      TAVILY_API_KEY: ${{ secrets.TAVILY_API_KEY }}
      COGNI_NODE_DBS: ${{ secrets.COGNI_NODE_DBS }}
    steps:
      # Deploy tooling (scripts/ci/deploy-infra.sh + scripts/ci/lib/**) runs at the
      # WORKFLOW ref, not the promoted app sha. The app stays pinned via COGNI_REPO_REF
      # (env above) and images by digest; deploy-infra.sh already pulls infra/compose
      # from `--ref main`. Pinning the orchestration script to head_sha stranded infra
      # fixes (e.g. the doltgres auth-file reconcile) until an app sha carrying them was
      # promoted — an infra-only fix could not reach prod on its own. github.sha = the
      # dispatched workflow ref, so tooling fixes apply on the next deploy regardless of
      # which app sha is promoted (consistent with the existing `--ref main` compose source).
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}

      - name: Setup SSH
        run: |
          if [ -z "$VM_HOST" ]; then
            echo "⚠️ No VM_HOST — skipping infra deploy"
            exit 0
          fi
          mkdir -p ~/.ssh && chmod 700 ~/.ssh
          echo "${{ secrets.SSH_DEPLOY_KEY }}" | tr -d '\r' > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -T 10 -H "$VM_HOST" >> ~/.ssh/known_hosts 2>/dev/null || true

      - name: Preflight Grafana PDC signing token
        if: env.VM_HOST != '' && env.GRAFANA_PDC_SIGNING_TOKEN != ''
        run: bash scripts/grafana-pdc-token-preflight.sh

      - name: Deploy infrastructure
        if: env.VM_HOST != ''
        run: |
          args=(--ref main)
          if [ "${{ inputs.deploy_infra_mode || 'full' }}" = "k8s-secrets-only" ]; then
            args+=(--k8s-secrets-only)
          fi
          scripts/ci/deploy-infra.sh "${args[@]}"

      - name: Provision Grafana Postgres datasources
        if: env.VM_HOST != '' && (inputs.deploy_infra_mode || 'full') != 'k8s-secrets-only'
        run: bash scripts/ci/provision-grafana-postgres-datasources.sh

      - name: Verify Grafana Postgres datasources
        if: env.VM_HOST != '' && (inputs.deploy_infra_mode || 'full') != 'k8s-secrets-only'
        continue-on-error: true
        run: bash scripts/ci/verify-grafana-postgres-datasources.sh

      - name: Upload deployment artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: infra-deployment-artifacts-${{ github.run_id }}
          path: ${{ runner.temp }}/deploy-infra-${{ github.run_id }}/
          if-no-files-found: warn
          retention-days: 14

      - name: CI Telemetry
        if: always()
        uses: ./.github/actions/loki-ci-telemetry
        with:
          loki_url: ${{ secrets.GRAFANA_CLOUD_LOKI_URL }}
          loki_user: ${{ secrets.GRAFANA_CLOUD_LOKI_USER }}
          loki_token: ${{ secrets.GRAFANA_CLOUD_LOKI_API_KEY }}
          job_status: ${{ job.status }}

  verify-deploy:
    # reconcile-dns ordered before the public /version probe so a newly-added node's
    # host resolves; its failure surfaces on reconcile-dns, the probe stays the gate.
    needs: [decide, promote-k8s, deploy-infra, reconcile-dns, node-substrate]
    if: |
      always() &&
      needs.promote-k8s.result == 'success' &&
      (
        (needs.decide.outputs.has_node_targets == 'true' && needs.node-substrate.result == 'success') ||
        (needs.decide.outputs.has_node_targets != 'true' && needs.node-substrate.result == 'skipped')
      ) &&
      (needs.deploy-infra.result == 'success' || needs.deploy-infra.result == 'skipped')
    strategy:
      fail-fast: false
      matrix:
        node: ${{ fromJson(needs.decide.outputs.targets_json) }}
    concurrency:
      group: verify-deploy-${{ needs.decide.outputs.environment }}-${{ matrix.node }}
      cancel-in-progress: false
    runs-on: ubuntu-latest
    environment: ${{ needs.decide.outputs.environment }}
    env:
      VM_HOST: ${{ secrets.VM_HOST }}
      DOMAIN: ${{ vars.DOMAIN }}
      DEPLOY_ENVIRONMENT: ${{ needs.decide.outputs.environment }}
    steps:
      - name: Checkout (workspace root for composite actions)
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.decide.outputs.head_sha }}

      - name: Checkout app source
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.decide.outputs.head_sha }}
          path: app-src

      - name: Checkout per-node deploy branch
        uses: actions/checkout@v4
        with:
          ref: deploy/${{ needs.decide.outputs.environment }}-${{ matrix.node }}
          path: deploy-branch
          token: ${{ secrets.ACTIONS_AUTOMATION_BOT_PAT }}

      - name: Download per-cell artifact
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.1.8
        with:
          name: cell-${{ matrix.node }}
          path: ${{ runner.temp }}/

      - name: Resolve cell state
        id: cell
        env:
          NODE: ${{ matrix.node }}
          CELL_DIR: ${{ runner.temp }}
          ENVIRONMENT: ${{ needs.decide.outputs.environment }}
        run: bash app-src/scripts/ci/resolve-cell-state.sh

      - name: Setup SSH
        if: steps.cell.outputs.promoted == 'true'
        env:
          SSH_DEPLOY_KEY: ${{ secrets.SSH_DEPLOY_KEY }}
        run: |
          mkdir -p ~/.ssh && chmod 700 ~/.ssh
          echo "$SSH_DEPLOY_KEY" | tr -d '\r' > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -T 10 -H "$VM_HOST" >> ~/.ssh/known_hosts 2>/dev/null || true

      - name: Wait for ArgoCD sync (per-node)
        if: steps.cell.outputs.promoted == 'true'
        env:
          SSH_OPTS: "-i ~/.ssh/deploy_key -o StrictHostKeyChecking=accept-new -o ConnectTimeout=30 -o ServerAliveInterval=10 -o ServerAliveCountMax=6"
          EXPECTED_SHA: ${{ steps.cell.outputs.deploy_branch_sha }}
          PROMOTED_APPS: ${{ matrix.node }}
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: bash app-src/scripts/ci/wait-for-argocd.sh

      - name: Wait for in-cluster services (per-node)
        if: steps.cell.outputs.promoted == 'true'
        env:
          PROMOTED_APPS: ${{ matrix.node }}
        run: bash app-src/scripts/ci/wait-for-in-cluster-services.sh

      - name: Verify buildSha (per-node)
        if: steps.cell.outputs.promoted == 'true'
        env:
          SOURCE_SHA_MAP: deploy-branch/.promote-state/source-sha-by-app.json
          EXPECTED_BUILDSHA: ${{ needs.decide.outputs.build_sha }}
          NODES: ${{ matrix.node }}
          MARKER_DIR: out
        run: bash app-src/scripts/ci/verify-buildsha.sh

      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: steps.cell.outputs.promoted == 'true'
        with:
          name: cell-verify-${{ matrix.node }}
          path: out/
          retention-days: 1

      - name: CI Telemetry
        if: always()
        uses: ./.github/actions/loki-ci-telemetry
        with:
          loki_url: ${{ secrets.GRAFANA_CLOUD_LOKI_URL }}
          loki_user: ${{ secrets.GRAFANA_CLOUD_LOKI_USER }}
          loki_token: ${{ secrets.GRAFANA_CLOUD_LOKI_API_KEY }}
          job_status: ${{ job.status }}

  verify:
    runs-on: ubuntu-latest
    needs: [decide, promote-k8s, deploy-infra]
    if: |
      always() &&
      needs.promote-k8s.result == 'success' &&
      (needs.deploy-infra.result == 'success' || needs.deploy-infra.result == 'skipped')
    environment: ${{ needs.decide.outputs.environment }}
    env:
      VM_HOST: ${{ secrets.VM_HOST }}
      DOMAIN: ${{ vars.DOMAIN }}
      K8S_NAMESPACE: cogni-${{ needs.decide.outputs.environment }}
      PROMOTED_APPS: ${{ join(fromJson(needs.decide.outputs.node_targets_json), ',') }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.decide.outputs.head_sha }}

      - name: Verify deployment
        run: |
          if [ -z "$DOMAIN" ]; then
            echo "⚠️ No DOMAIN — skipping verify"
            exit 0
          fi
          scripts/ci/verify-deployment.sh

      - name: Dump diagnostics on failure
        if: failure() && env.VM_HOST != ''
        env:
          SSH_DEPLOY_KEY: ${{ secrets.SSH_DEPLOY_KEY }}
        run: |
          mkdir -p ~/.ssh && chmod 700 ~/.ssh
          echo "$SSH_DEPLOY_KEY" | tr -d '\r' > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -T 10 -H "$VM_HOST" >> ~/.ssh/known_hosts 2>/dev/null || true
          SSH_OPTS="-i ~/.ssh/deploy_key -o StrictHostKeyChecking=accept-new -o ConnectTimeout=10"
          # shellcheck disable=SC2086
          ssh $SSH_OPTS root@"$VM_HOST" "kubectl -n argocd get applications -o wide" 2>&1 || true
          # shellcheck disable=SC2086
          ssh $SSH_OPTS root@"$VM_HOST" "kubectl -n $K8S_NAMESPACE get pods -o wide" 2>&1 || true
          # shellcheck disable=SC2086
          ssh $SSH_OPTS root@"$VM_HOST" "kubectl -n $K8S_NAMESPACE get events --sort-by=.lastTimestamp | tail -20" 2>&1 || true

      - name: CI Telemetry
        if: always()
        uses: ./.github/actions/loki-ci-telemetry
        with:
          loki_url: ${{ secrets.GRAFANA_CLOUD_LOKI_URL }}
          loki_user: ${{ secrets.GRAFANA_CLOUD_LOKI_USER }}
          loki_token: ${{ secrets.GRAFANA_CLOUD_LOKI_API_KEY }}
          job_status: ${{ job.status }}

  e2e:
    runs-on: ubuntu-latest
    needs: [decide, promote-k8s, verify, verify-deploy]
    if: |
      needs.promote-k8s.result == 'success' &&
      needs.verify.result == 'success' &&
      needs.verify-deploy.result == 'success'
    timeout-minutes: 10
    environment: ${{ needs.decide.outputs.environment }}
    env:
      NEXT_TELEMETRY_DISABLED: 1
      TEST_BASE_URL: https://${{ vars.DOMAIN }}
    steps:
      - name: Check prerequisites
        id: gate
        run: |
          if [ -z "$TEST_BASE_URL" ] || [ "$TEST_BASE_URL" = "https://" ]; then
            echo "skip=true" >> "$GITHUB_OUTPUT"
            echo "⚠️ DOMAIN not set — skipping E2E"
          else
            echo "skip=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Checkout
        if: steps.gate.outputs.skip != 'true'
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.decide.outputs.head_sha }}

      - name: Setup Node.js
        if: steps.gate.outputs.skip != 'true'
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Setup pnpm
        if: steps.gate.outputs.skip != 'true'
        uses: pnpm/action-setup@v4

      - name: Install dependencies
        if: steps.gate.outputs.skip != 'true'
        run: pnpm install --frozen-lockfile

      - name: Cache Playwright browsers
        if: steps.gate.outputs.skip != 'true'
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-playwright-

      - name: Install Playwright browsers
        if: steps.gate.outputs.skip != 'true'
        run: pnpm exec playwright install --with-deps

      - name: Run Playwright smoke tests
        if: steps.gate.outputs.skip != 'true'
        run: pnpm e2e:smoke

      - name: Upload E2E artifacts
        if: always() && steps.gate.outputs.skip != 'true'
        uses: actions/upload-artifact@v4
        with:
          name: e2e-smoke-${{ github.run_id }}
          path: e2e/artifacts/
          if-no-files-found: warn
          retention-days: 7

      - name: CI Telemetry
        if: always()
        uses: ./.github/actions/loki-ci-telemetry
        with:
          loki_url: ${{ secrets.GRAFANA_CLOUD_LOKI_URL }}
          loki_user: ${{ secrets.GRAFANA_CLOUD_LOKI_USER }}
          loki_token: ${{ secrets.GRAFANA_CLOUD_LOKI_API_KEY }}
          job_status: ${{ job.status }}

  aggregate-preview:
    needs: [decide, promote-k8s, deploy-infra, verify, verify-deploy, e2e]
    # has_targets guard: skip the bug.0443 loud-fail backstop on a legit no-op
    # (no k8s targets). It still fires when has_targets==true but nothing
    # promoted (admin-merge silent-skip) — that's the case it must catch.
    if: always() && needs.decide.outputs.environment == 'preview' && needs.decide.outputs.has_targets == 'true'
    runs-on: ubuntu-latest
    concurrency:
      group: aggregate-preview
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 1
          token: ${{ secrets.ACTIONS_AUTOMATION_BOT_PAT }}

      - name: Download all per-cell artifacts
        if: always()
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.1.8
        with:
          pattern: cell-*
          path: ${{ runner.temp }}/cells/
          merge-multiple: true

      - name: Decide outcome
        id: outcome
        env:
          ENV: preview
          CELLS_DIR: ${{ runner.temp }}/cells
          PROMOTE_RESULT: ${{ needs.promote-k8s.result }}
          DEPLOY_INFRA_RESULT: ${{ needs.deploy-infra.result }}
          VERIFY_RESULT: ${{ needs.verify.result }}
          VERIFY_DEPLOY_RESULT: ${{ needs.verify-deploy.result }}
          E2E_RESULT: ${{ needs.e2e.result }}
        run: bash scripts/ci/aggregate-decide-outcome.sh

      - name: Update deploy/preview rollup (current-sha + source-sha-by-app)
        if: steps.outcome.outputs.outcome == 'dispatched'
        env:
          GH_TOKEN: ${{ secrets.ACTIONS_AUTOMATION_BOT_PAT }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          ROLLUP_TARGETS_JSON: ${{ needs.decide.outputs.targets_json }}
        run: bash scripts/ci/aggregate-rollup.sh preview

      - name: Emit preview-flight-outcome artifact
        if: always()
        run: printf '%s\n' "${{ steps.outcome.outputs.outcome }}" > preview-flight-outcome.txt
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: always()
        with:
          name: preview-flight-outcome
          path: preview-flight-outcome.txt

  aggregate-production:
    needs: [decide, promote-k8s, verify, verify-deploy, e2e]
    if: |
      always() &&
      needs.decide.outputs.environment == 'production' &&
      needs.promote-k8s.result == 'success' &&
      needs.verify.result == 'success' &&
      needs.verify-deploy.result == 'success' &&
      needs.e2e.result == 'success'
    runs-on: ubuntu-latest
    concurrency:
      group: aggregate-production
      cancel-in-progress: false
    steps:
      # Production aggregator scripts are pinned to main so a
      # workflow_dispatch from any other ref cannot inject scripts that
      # write to deploy/production. Branch protection on main is the
      # gate; the dispatched workflow YAML can call only what main ships.
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 1
          token: ${{ secrets.ACTIONS_AUTOMATION_BOT_PAT }}

      - name: Download all per-cell artifacts
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.1.8
        with:
          pattern: cell-*
          path: ${{ runner.temp }}/cells/
          merge-multiple: true

      - name: Assert promoted cells were verified
        env:
          ENV: production
          CELLS_DIR: ${{ runner.temp }}/cells
          PROMOTE_RESULT: ${{ needs.promote-k8s.result }}
          VERIFY_RESULT: ${{ needs.verify.result }}
          VERIFY_DEPLOY_RESULT: ${{ needs.verify-deploy.result }}
          E2E_RESULT: ${{ needs.e2e.result }}
          STRICT_FAIL: "1"
        run: bash scripts/ci/aggregate-decide-outcome.sh

      - name: Update deploy/production rollup (current-sha + source-sha-by-app)
        env:
          GH_TOKEN: ${{ secrets.ACTIONS_AUTOMATION_BOT_PAT }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          ROLLUP_TARGETS_JSON: ${{ needs.decide.outputs.targets_json }}
        run: bash scripts/ci/aggregate-rollup.sh production
