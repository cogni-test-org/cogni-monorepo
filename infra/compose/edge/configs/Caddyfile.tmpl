# GENERATED by scripts/ci/render-caddyfile.sh from infra/catalog/*.yaml —
# DO NOT EDIT BY HAND. Add a node = add its catalog entry (with node_port) and
# run `pnpm gen:caddyfile`. CI (`scripts/ci/render-caddyfile.sh --check`) fails
# if this file drifts from the catalog. See docs/guides/create-node.md.
{
  log {
    level INFO
    format json
    output file /data/logs/caddy/caddy.log {
      roll_size 10MB
      roll_keep 7
      roll_keep_for 168h
    }
  }
}

# Redirect www to non-www (canonical operator domain)
www.{$DOMAIN} {
  redir https://{$DOMAIN}{uri} permanent
}

# ── operator (primary domain) → k3s NodePort 30000 ──────────────────────
{$DOMAIN} {
  encode zstd gzip

  @public path /api/v1/public/*
  handle @public {
    reverse_proxy {$OPERATOR_UPSTREAM:app:3000} {
      header_up X-Real-IP {remote_host}
      transport http {
        response_header_timeout 10s
      }
    }
  }

  reverse_proxy {$OPERATOR_UPSTREAM:app:3000}

  log {
    format json
    output file /data/logs/caddy/access.log {
      roll_size 10MB
      roll_keep 7
      roll_keep_for 168h
    }
  }
}

# ── anotha node → k3s NodePort 30900 ──────────────────────────────────
{$ANOTHA_DOMAIN:anotha.localhost} {
  encode zstd gzip

  @public path /api/v1/public/*
  handle @public {
    reverse_proxy {$ANOTHA_UPSTREAM:host.docker.internal:30900} {
      header_up X-Real-IP {remote_host}
      transport http {
        response_header_timeout 10s
      }
    }
  }

  reverse_proxy {$ANOTHA_UPSTREAM:host.docker.internal:30900}

  log {
    format json
    output file /data/logs/caddy/access-anotha.log {
      roll_size 10MB
      roll_keep 7
      roll_keep_for 168h
    }
  }
}

# ── brother node → k3s NodePort 30700 ──────────────────────────────────
{$BROTHER_DOMAIN:brother.localhost} {
  encode zstd gzip

  @public path /api/v1/public/*
  handle @public {
    reverse_proxy {$BROTHER_UPSTREAM:host.docker.internal:30700} {
      header_up X-Real-IP {remote_host}
      transport http {
        response_header_timeout 10s
      }
    }
  }

  reverse_proxy {$BROTHER_UPSTREAM:host.docker.internal:30700}

  log {
    format json
    output file /data/logs/caddy/access-brother.log {
      roll_size 10MB
      roll_keep 7
      roll_keep_for 168h
    }
  }
}

# ── canary node → k3s NodePort 30400 ──────────────────────────────────
{$CANARY_DOMAIN:canary.localhost} {
  encode zstd gzip

  @public path /api/v1/public/*
  handle @public {
    reverse_proxy {$CANARY_UPSTREAM:host.docker.internal:30400} {
      header_up X-Real-IP {remote_host}
      transport http {
        response_header_timeout 10s
      }
    }
  }

  reverse_proxy {$CANARY_UPSTREAM:host.docker.internal:30400}

  log {
    format json
    output file /data/logs/caddy/access-canary.log {
      roll_size 10MB
      roll_keep 7
      roll_keep_for 168h
    }
  }
}

# ── ghcr node → k3s NodePort 30800 ──────────────────────────────────
{$GHCR_DOMAIN:ghcr.localhost} {
  encode zstd gzip

  @public path /api/v1/public/*
  handle @public {
    reverse_proxy {$GHCR_UPSTREAM:host.docker.internal:30800} {
      header_up X-Real-IP {remote_host}
      transport http {
        response_header_timeout 10s
      }
    }
  }

  reverse_proxy {$GHCR_UPSTREAM:host.docker.internal:30800}

  log {
    format json
    output file /data/logs/caddy/access-ghcr.log {
      roll_size 10MB
      roll_keep 7
      roll_keep_for 168h
    }
  }
}

# ── liltoken node → k3s NodePort 31000 ──────────────────────────────────
{$LILTOKEN_DOMAIN:liltoken.localhost} {
  encode zstd gzip

  @public path /api/v1/public/*
  handle @public {
    reverse_proxy {$LILTOKEN_UPSTREAM:host.docker.internal:31000} {
      header_up X-Real-IP {remote_host}
      transport http {
        response_header_timeout 10s
      }
    }
  }

  reverse_proxy {$LILTOKEN_UPSTREAM:host.docker.internal:31000}

  log {
    format json
    output file /data/logs/caddy/access-liltoken.log {
      roll_size 10MB
      roll_keep 7
      roll_keep_for 168h
    }
  }
}

# ── node-template node → k3s NodePort 30200 ──────────────────────────────────
{$NODE_TEMPLATE_DOMAIN:node-template.localhost} {
  encode zstd gzip

  @public path /api/v1/public/*
  handle @public {
    reverse_proxy {$NODE_TEMPLATE_UPSTREAM:host.docker.internal:30200} {
      header_up X-Real-IP {remote_host}
      transport http {
        response_header_timeout 10s
      }
    }
  }

  reverse_proxy {$NODE_TEMPLATE_UPSTREAM:host.docker.internal:30200}

  log {
    format json
    output file /data/logs/caddy/access-node-template.log {
      roll_size 10MB
      roll_keep 7
      roll_keep_for 168h
    }
  }
}

# ── resy node → k3s NodePort 30300 ──────────────────────────────────
{$RESY_DOMAIN:resy.localhost} {
  encode zstd gzip

  @public path /api/v1/public/*
  handle @public {
    reverse_proxy {$RESY_UPSTREAM:host.docker.internal:30300} {
      header_up X-Real-IP {remote_host}
      transport http {
        response_header_timeout 10s
      }
    }
  }

  reverse_proxy {$RESY_UPSTREAM:host.docker.internal:30300}

  log {
    format json
    output file /data/logs/caddy/access-resy.log {
      roll_size 10MB
      roll_keep 7
      roll_keep_for 168h
    }
  }
}

# ── test-cog node → k3s NodePort 30600 ──────────────────────────────────
{$TEST_COG_DOMAIN:test-cog.localhost} {
  encode zstd gzip

  @public path /api/v1/public/*
  handle @public {
    reverse_proxy {$TEST_COG_UPSTREAM:host.docker.internal:30600} {
      header_up X-Real-IP {remote_host}
      transport http {
        response_header_timeout 10s
      }
    }
  }

  reverse_proxy {$TEST_COG_UPSTREAM:host.docker.internal:30600}

  log {
    format json
    output file /data/logs/caddy/access-test-cog.log {
      roll_size 10MB
      roll_keep 7
      roll_keep_for 168h
    }
  }
}

# ── yo node → k3s NodePort 30500 ──────────────────────────────────
{$YO_DOMAIN:yo.localhost} {
  encode zstd gzip

  @public path /api/v1/public/*
  handle @public {
    reverse_proxy {$YO_UPSTREAM:host.docker.internal:30500} {
      header_up X-Real-IP {remote_host}
      transport http {
        response_header_timeout 10s
      }
    }
  }

  reverse_proxy {$YO_UPSTREAM:host.docker.internal:30500}

  log {
    format json
    output file /data/logs/caddy/access-yo.log {
      roll_size 10MB
      roll_keep 7
      roll_keep_for 168h
    }
  }
}
