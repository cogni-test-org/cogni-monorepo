apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: spawny-boi-compute-env-secrets-reader
  namespace: cogni-candidate-a
rules:
  - apiGroups:
      - ""
    resources:
      - secrets
    resourceNames:
      - spawny-boi-compute-env-secrets
    verbs:
      - get
