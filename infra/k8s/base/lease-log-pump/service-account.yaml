# SPDX-License-Identifier: LicenseRef-PolyForm-Shield-1.0.0
# SPDX-FileCopyrightText: 2025 Cogni-DAO
#
# Deliberately Role-less (bug.5240). The pump's coverage comes from the actuator's ledger,
# not the Kubernetes API — binding it nothing is the point, not an omission.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: lease-log-pump
  labels:
    app.kubernetes.io/name: lease-log-pump
    app.kubernetes.io/part-of: cogni
