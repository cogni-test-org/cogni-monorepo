# SPDX-License-Identifier: LicenseRef-PolyForm-Shield-1.0.0
# SPDX-FileCopyrightText: 2025 Cogni-DAO
#
# GENERATED by scripts/ci/render-node-appset.sh — DO NOT EDIT BY HAND.
# One ApplicationSet per (env, node) for structural LANE_ISOLATION (axiom 18).
# Regenerate: pnpm gen:node-appset
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: cogni-candidate-a-node-template
  namespace: argocd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: https://github.com/cogni-dao/cogni.git
        revision: deploy/candidate-a-node-template
        files:
          - path: "infra/catalog/node-template.yaml"
  template:
    metadata:
      name: "candidate-a-{{.name}}"
      namespace: argocd
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: default
      source:
        repoURL: https://github.com/cogni-dao/cogni.git
        targetRevision: "deploy/candidate-a-{{.name}}"
        path: "infra/k8s/overlays/candidate-a/{{.name}}"
      destination:
        server: https://kubernetes.default.svc
        namespace: cogni-candidate-a
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
