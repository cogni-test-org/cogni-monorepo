# SPDX-License-Identifier: LicenseRef-PolyForm-Shield-1.0.0
# SPDX-FileCopyrightText: 2025 Cogni-DAO
#
# Spec: work/items/task.0372 (per-node lane isolation, GR-1 four-generator shape).
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: cogni-candidate-a
  namespace: argocd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: https://github.com/cogni-dao/cogni.git
        revision: deploy/candidate-a-operator
        files:
          - path: "infra/catalog/operator.yaml"
    - git:
        repoURL: https://github.com/cogni-dao/cogni.git
        revision: deploy/candidate-a-resy
        files:
          - path: "infra/catalog/resy.yaml"
    - git:
        repoURL: https://github.com/cogni-dao/cogni.git
        revision: deploy/candidate-a-scheduler-worker
        files:
          - path: "infra/catalog/scheduler-worker.yaml"
    - git:
        repoURL: https://github.com/cogni-dao/cogni.git
        revision: deploy/candidate-a-node-template
        files:
          - path: "infra/catalog/node-template.yaml"
  template:
    metadata:
      name: "candidate-a-{{.name}}"
      namespace: argocd
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: default
      source:
        repoURL: https://github.com/cogni-dao/cogni.git
        targetRevision: "deploy/candidate-a-{{.name}}"
        path: "infra/k8s/overlays/candidate-a/{{.name}}"
      destination:
        server: https://kubernetes.default.svc
        namespace: cogni-candidate-a
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
