# SPDX-License-Identifier: LicenseRef-PolyForm-Shield-1.0.0
# SPDX-FileCopyrightText: 2025 Cogni-DAO
#
# GENERATED by scripts/ci/render-node-appset.sh — DO NOT EDIT BY HAND.
# One ApplicationSet per (env, node) for structural LANE_ISOLATION (axiom 18).
# Regenerate: pnpm gen:node-appset
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: cogni-production-brother
  namespace: argocd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: https://github.com/cogni-dao/cogni.git
        revision: deploy/production-brother
        files:
          - path: "infra/catalog/brother.yaml"
  template:
    metadata:
      name: "production-{{.name}}"
      namespace: argocd
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: default
      source:
        repoURL: https://github.com/cogni-dao/cogni.git
        targetRevision: "deploy/production-{{.name}}"
        path: "infra/k8s/overlays/production/{{.name}}"
      destination:
        server: https://kubernetes.default.svc
        namespace: cogni-production
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          # SSA makes Argo a field manager so a hand-edit to a live pod-template
          # field (e.g. serviceAccountName) is reconciled by selfHeal, not left as
          # an unmanaged field client-side 3-way merge can't see.
          - ServerSideApply=true
