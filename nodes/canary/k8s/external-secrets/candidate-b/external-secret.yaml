# SPDX-License-Identifier: LicenseRef-PolyForm-Shield-1.0.0
# SPDX-FileCopyrightText: 2025 Cogni-DAO
#
# ExternalSecret for canary on candidate-b. One service/env path is extracted
# from OpenBao into the single k8s Secret consumed by the workload.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: canary-env-secrets
  namespace: cogni-candidate-b
  labels:
    app.kubernetes.io/part-of: cogni-secrets-substrate
    app.kubernetes.io/component: canary
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao-backend
    kind: ClusterSecretStore
  target:
    name: canary-env-secrets
    creationPolicy: Owner
    deletionPolicy: Retain
  dataFrom:
    - extract:
        key: candidate-b/canary
