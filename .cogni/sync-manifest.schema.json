{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/cogni-dao/cogni/.cogni/sync-manifest.schema.json",
  "title": "Cogni multi-repo sync manifest (inverse form v2)",
  "description": "spec.repo-sync-contract — inverse manifest: declares global excludes + per-artifact divergences (omit_from_artifact + artifact_only). Anything else under hub is expected to mirror 1:1 across all artifacts. Mirrors the CATALOG_IS_SSOT pattern (infra/catalog/_schema.json).",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "hub", "artifacts", "exclude", "divergences"],
  "properties": {
    "node_local": {
      "type": "array",
      "uniqueItems": true,
      "items": { "type": "string", "minLength": 1 },
      "description": "Tier 3 (node identity / presentation) globs — homepage, landing UI, branding/theme, repo-spec, persona. Carved OUT of the node-template→fork Tier-2 substrate merge: node-template is a starter only, so its presentation NEVER overwrites a fork's (spec.repo-sync-contract THREE_TIER_CARVE_OUT). Read at runtime by the operator fork-sync from node-template's copy of this manifest (TIER3_IS_DATA). Distinct axis from divergences[] (hub↔artifact drift)."
    },
    "schema": {
      "const": 2,
      "description": "Manifest schema version. Bump = lock-step PRs across all repos per MANIFEST_BOOTSTRAP."
    },
    "hub": {
      "type": "string",
      "pattern": "^[A-Za-z0-9._-]+/[A-Za-z0-9._-]+$",
      "description": "GitHub owner/repo of the canonical hub. Per HUB_IS_COGNI_MONOREPO this is cogni-dao/cogni."
    },
    "artifacts": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["repo", "visibility"],
        "properties": {
          "repo": {
            "type": "string",
            "pattern": "^[A-Za-z0-9._-]+/[A-Za-z0-9._-]+$"
          },
          "visibility": {
            "enum": ["public", "private"],
            "description": "public = anonymous clone OK in detector; private = needs PAT (detector skips in v0.1)."
          },
          "role": {
            "enum": ["fork", "test-parent"],
            "default": "fork",
            "description": "What this artifact IS to the hub. `fork` (default) = a downstream artifact that carries its own node body. `test-parent` = a near-1:1 CI/CD mirror of the hub whose ONLY sanctioned differences are declared test identity, fixture roster/pins, and environment-specific desired state (spec.repo-sync-contract TEST_PARENT_IS_NEAR_IDENTICAL). Consumed by scripts/ci/sync-test-parent.mjs to pick its target — never a second hardcoded owner/repo."
          },
          "on_missing": {
            "enum": ["report", "fail"],
            "default": "report",
            "description": "What an UNDECLARED missing-on-artifact path means. `report` (default) = drift is data; the detector exits 0 and the tracking issue carries it. `fail` = the detector exits non-zero. A test parent sets `fail`: a canonical path the hub generates and the mirror lacks is not a report, it is a broken generator input (task.5142 — the absent infra/k8s/overlays/<env>/scheduler-worker/node-endpoints.patch.yaml that 422'd spawny-boi env activation)."
          },
          "github_app": {
            "type": "object",
            "additionalProperties": false,
            "required": ["id", "slug"],
            "properties": {
              "id": {
                "type": "string",
                "pattern": "^[0-9]+$",
                "description": "Non-secret GitHub App ID allowed to write this artifact."
              },
              "slug": {
                "type": "string",
                "pattern": "^[A-Za-z0-9-]+$",
                "description": "Expected App slug returned after token mint; runtime owner-to-App fail-closed boundary."
              }
            }
          }
        },
        "allOf": [
          {
            "if": {
              "required": ["role"],
              "properties": { "role": { "const": "test-parent" } }
            },
            "then": { "required": ["github_app"] }
          }
        ]
      }
    },
    "exclude": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": { "type": "string", "minLength": 1 },
      "description": "Global glob excludes — never propagated to or from any artifact. Junk/caches/build outputs."
    },
    "divergences": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["artifact", "reason"],
        "properties": {
          "artifact": {
            "type": "string",
            "pattern": "^[A-Za-z0-9._-]+/[A-Za-z0-9._-]+$"
          },
          "reason": {
            "type": "string",
            "minLength": 1,
            "description": "Human-readable justification per DECLARED_DIVERGENCE."
          },
          "omit_from_artifact": {
            "type": "array",
            "uniqueItems": true,
            "items": { "type": "string", "minLength": 1 },
            "description": "Globs of paths the hub has but this artifact MUST NOT carry."
          },
          "artifact_only": {
            "type": "array",
            "uniqueItems": true,
            "items": { "type": "string", "minLength": 1 },
            "description": "Globs of paths the artifact has but the hub MUST NOT carry."
          },
          "content_may_differ": {
            "type": "array",
            "uniqueItems": true,
            "items": { "type": "string", "minLength": 1 },
            "description": "Globs of paths that MUST EXIST on both sides but whose CONTENT is legitimately per-deployment (identity, roster-generated desired state). Suppresses the content check only — a missing path is still drift, so a generated canonical path can never go silently absent. TAKES PRECEDENCE over omit_from_artifact, so a broad roster omission can re-require the control-plane rows inside it. Replaces the v1 twin-listing workaround (the same glob in omit_from_artifact AND artifact_only), which zeroed the flag in both directions and therefore hid absence too."
          }
        }
      }
    }
  }
}
