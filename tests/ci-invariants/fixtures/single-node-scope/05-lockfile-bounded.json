{
  "name": "lockfile-inherits is bounded: legacy-alpha + lockfile + .github/foo defeats it",
  "paths": [
    "nodes/legacy-alpha/app/package.json",
    "pnpm-lock.yaml",
    ".github/workflows/foo.yml"
  ],
  "expected": {
    "domains": ["legacy-alpha", "operator"],
    "pass": false,
    "rideAlongApplied": false
  }
}
